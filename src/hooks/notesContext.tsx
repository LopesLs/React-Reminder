import { createContext } from "react";

const NotesContext = createContext({}); // Cria um contexto para compartilhar dados entre componentes

export default NotesContext; // Exporta o contexto para ser usado em outros componentes